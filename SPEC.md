# 살피 (SALPI) 명세

한 줄 정의: 전자금융감독규정 시행세칙 제2조의3이 만든 망분리 예외 일곱 갈래의 전체 생애주기, 곧 등재 판정, 운영 중 대조와 검사, 반기 보고를 한 도구로 관리한다. 승인된 통로(콘딧)를 대장으로 세고, 실제 트래픽과 대조해 대장에 없는 통로를 찾고, SaaS 통로는 지나가는 내용까지 검사한다.

이름의 뜻: 살피는 두 땅의 경계를 나타내던 순우리말 표지다. '살피다'와 어원이 같다.

## 구성요소

| 코드 | 이름 | 역할 |
|---|---|---|
| C | 콘딧 관제 | 예외 통로의 대장, 경계 지도, 로그 대조, 반기 보고 조립 |
| W | 판정 워크벤치 | SaaS 예외(제1항 3호)의 도입 전 적격 검토, 조치 제안, 검토서 생성 |
| D | 검증 엔진 | 개인신용정보 유입의 실시간과 주기 탐지 (3층) |
| E | 근거 추출 | 벤더 공개 문서에서 통제 수단을 원문 인용과 함께 추출 |
| L | 자가보강 루프 | 검증 엔진의 성능 측정과 개선. 생성이 곧 라벨 |

C가 뼈대이고 W, D, E, L이 그 위에 얹힌다. W는 새 SaaS가 대장에 오르는 관문, D는 SaaS 콘딧의 운영 통제, E는 W의 근거 공급원, L은 D의 측정 장치다.

## 파이프라인

### A. 등재 (C + W + E)

```
로그 대조가 미등록 SaaS 발견 (또는 담당자가 직접 등재 시작)
  → [E] LLM이 공개 관리자 문서와 DPA에서 층별 통제 수단 추출 (원문 인용 필수)
  → 담당자 확인 체크 (사람 게이트)
  → 결정 트리 판정 (결정적)
       고객정보가 업무 목적 / 필드형 기능 사용 → 불가
       유입 가능성 등급 × 통제 상태 → 적합 / 조건부 / 불가
  → 미해소 위험마다 조치 제안 (콘솔 설정 경로, 보완통제 지정, 내규 문안)
  → 적합이나 조건부면 대장 등재(위원회 상정), 불가면 차단 목록
```

### B. 운영 (C + D)

```
통로 감시: 방화벽과 프록시 로그 → 목적지 추출 → 대장과 집합 대조 (결정적)
           대장에 없는 목적지 → LLM이 분류와 위험 사유 → 등재 판정 또는 차단
내용 감시: SaaS 콘딧 한정 3형태
           실시간   크롬 확장. 입력창 감시, 경고 배너
           수시     통합 앱 내용 검사 탭. 문서 붙여넣기 검사
           주기     배치 스캐너. 저장 문서 스캔, 증적 리포트
탐지 기록은 마스킹되어 해당 콘딧에 누적 → 반기 보고의 증적
```

W와 D의 결합점: 벤더가 통제를 제공하지 않으면(예: Notion에 DLP 없음) 검토서가 D를 보완통제로 지정한다. 해설서의 "기능이 없는 경우 보완 보안대책 적용"의 실물이다.

### C. 보고 (C)

```
대장 현황 + SaaS 반기 자체평가(제4항) + 내용검사 증적 + 미등록 발견과 조치 내역
  → 정보보호위원회 제출 초안 자동 조립
```

반기 자체평가 의무는 법문상 SaaS 예외(제1항 3호)에만 붙는다. 보고서도 그 구분을 유지한다.

### D. 자가보강 루프 (L)

```
생성기: 합성 금융 문서 생성, 위반 심은 위치가 곧 정답지 (생성이 곧 라벨)
  → 검증 엔진 스캔 결과와 대조 → TP/FN/FP → 정밀도와 재현율
  → 놓친 유형을 생성기 목표로 재투입 (어려운 케이스 증식) → 규칙과 프롬프트 보강
산출: 방법별 정확도 표, 세대별 재현율 곡선, 합성 데이터셋 공개
```

## 검증 엔진의 3층

| 층 | 대상 | 방법 | 위치 |
|---|---|---|---|
| 규칙 | 주민번호(체크섬), 카드(Luhn), 전화 | 정규식 + 검증, 결정적 | 브라우저 가능 |
| 식별정보 | 이름 등 | 호칭 패턴 + 지시어 제외 | 로컬 |
| 결합 | 식별정보 × 거래, 대출, 연체, 상담 문맥 | 소형 LLM (기본 Kanana-2-3B, 대조 Mi:dm 2.0 Base) | 로컬 (Ollama) |

위반 확정 규칙은 신용정보법 제2조의 코드화다. 고유식별정보 검출은 그 자체로 위반, 식별정보 단독은 통과(기록만), 식별정보와 신용정보의 결합은 위반. 셋째 줄이 기존 DLP가 구조적으로 못 하고 LLM이 필요한 지점이다.

## 불변 원칙

1. 판정과 위반 확정은 결정적 로직만. LLM 출력은 후보와 근거이며, 사람 확인 또는 규칙 검증을 통과한 뒤에만 효력을 갖는다
2. 모든 AI 출력은 원문 인용 필수. 인용이 원문에 없으면 폐기
3. 민감정보는 로컬에서만 처리한다. 데이터를 모델이 있는 곳으로 보내지 않고, 모델을 데이터가 있는 단말에 둔다

## 배포

```
실배포: 금융사 내부망, Ollama + 소형 모델, 통합 앱 + 브라우저 확장 + 배치 스캐너
데모:   정적 호스팅 + 중계 서버(키 은닉, 속도 제한) + 호스팅 오픈모델(공급자 이중화)
        규칙층은 브라우저 안에서 돌므로 서버가 전부 죽어도 실시간 탐지는 유지된다
        모델층 장애 시 규칙층 + 안내로 강등. 확장은 실동작 영상으로 증명
```

## 대회 전략 (2025 수상 패턴 반영)

- 조치 제안까지: 판정에서 끝나지 않고 설정 경로, 보완통제, 내규 문안을 제안한다 (2025 대상 팀의 승리 공식)
- 회사마다 다른 조치: 업권과 요금제 보유에 따라 같은 SaaS라도 판정과 조치가 달라진다
- 국산 모델: 기본값이 카카오 Kanana-2-3B, 대조군이 KT Mi:dm 2.0 Base. 2025년 Mi:dm 사용 팀이 금보원 원장상을 받은 선례가 있고, 여기에 정량 비교를 얹는다
- 보호 대상의 분리: 사용자는 보안 담당자, 보호 대상은 금융소비자의 개인신용정보
- 주제 계열: 공고 예시 6(이상거래 탐지)과 7(AI 보안 대응 비서)의 금융회사 보안 실무 지원 계열
- 과녁: 본선 11팀 진입 → 금보원 원장상과 공동개최사 대표이사상 권역

## 산출물

| 출구 | 내용 | 마감 |
|---|---|---|
| 금융 AI Challenge | 통합 앱 데모 URL + 기획서(정확도 표, 루프 곡선) + 기능명세서 | 9/7 10:00 |
| CISC-W'26 논문 | 결합 탐지 방법 비교 + 3층 구조 + SaaS 통제 실측 | 9월 말 추정 |
| 공개 | 규칙층 룰팩, 합성 데이터셋, 검토서 서식 | 수시 |

## 진행 현황

1. 완료. 검증 엔진 규칙층과 결합층, 측정 1회전 (F1 85.4, 유형별 표 확보)
2. 완료. 조치 제안, 회사 프로필, 워크벤치에 검증 통합
3. 완료. 크롬 확장(실시간), 배치 스캐너 + 증적 리포트(주기)
4. 완료. 콘딧 관제 통합: 경계 지도, 대장, 로그 대조, 등재 판정, 반기 보고 조립
5. 남음. 자가보강 루프 세대 반복(재현율 곡선), 데모 배포, Mi:dm 대조 측정,
   확장 실동작 영상, 기획서와 기능명세서 작성

## 모델 선택 근거 (2026.8 조사)

| 모델 | 라이선스 | 상업 이용 | 역할 |
|---|---|---|---|
| Kanana-2-3B (카카오, 2026.07) | Kanana Open | 조건부 | 기본값. 국내 최신, Q4 2.2GB. 한국어와 코딩이 강점, 수학이 약점. 과제와 방향 일치 |
| Mi:dm 2.0 Base (KT, 11.5B) | MIT | 가능 | 대조군. Ko-IFEval 82, Q4 7GB |
| Tri-21B-Think (트릴리온랩스, 2026.02) | Apache 2.0 | 가능 | 예산 내 최대. 사고형이라 JSON 후처리 필요 |
| Qwen3-4B-Instruct-2507 | Apache 2.0 | 가능 | 범용 다국어 기준선. 비사고 전용이라 JSON 강제와 충돌 없음 |
| EXAONE 3.5 2.4B (LG) | EXAONE License (NC) | 불가 | 한국어 비교군. 대회와 연구에는 쓸 수 있으나 제품 기본값 불가 |
| Gemma 3 4B | Gemma Terms of Use | 조건부 | 비교군. Apache 아님, 배포 시 법무 확인 필요 |

라이선스와 용도의 구분: 대회 출품과 논문은 상업적 이용이 아니므로 EXAONE의 비상업 조항이 막지 않는다. 막는 것은 제품화 국면이다. Kanana Open도 자기 서비스 운영은 자유이고(4.2조) 제3자에게 API, 온프레미스, 온디바이스로 재판매할 때만 별도 계약이 필요하다(4.1조). 제약이 아예 없는 것은 MIT인 Mi:dm 2.0 Base이며, 제품화 단계의 재검토 대상이다.

주의: `format:"json"`은 Ollama가 디코딩 단계에서 강제하므로 어떤 모델이든 형식은 깨지지 않는다. 따라서 모델 비교 지표는 "JSON이 나오는가"가 아니라 (1) 라벨 정확도, (2) 근거 문자열이 원문의 부분 문자열로 정확히 일치하는 비율이어야 한다. 소형 모델의 실패는 후자에서 드러난다.

## 측정 결과 (합성 30건, 위반 84건, seed=20260810, Kanana-2-3B)

| 방법 | 정밀도 | 재현율 | F1 |
|---|---|---|---|
| 규칙층만 | 100.0% | 42.9% | 60.0% |
| 규칙 + 근사 규칙 | 100.0% | 59.5% | 74.6% |
| 규칙 + LLM | 86.8% | 78.6% | 82.5% |
| 합집합 (실서비스) | 87.5% | 83.3% | 85.4% |

유형별 재현율(합집합): 주민번호 100%, 카드 100%, 같은 줄 결합 92.9%, 줄 넘김 결합 57.1%(규칙 0%), 지시어 결합 65.0%(규칙 5%).

규칙이 구조적으로 0%인 두 유형이 전체 위반의 40%다. 이것이 LLM 투입의 정량 근거다. 근사 규칙과 LLM은 대체 관계가 아니라 보완 관계다. 근사 규칙은 같은 줄에, LLM은 줄 넘김과 지시어에 강하다.
