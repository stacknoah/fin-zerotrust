# 결계 (GYEOLGYE / WARD) — 프로젝트 명세

WARD: Workbench for Assessment and Runtime Detection

한 줄 정의: 시행세칙 제2조의3 제1항 제3호의 "이용자의 고유식별정보·개인신용정보를 처리하지 않을 것" 요건을, 도입 전에는 판정 가능하게(W), 운영 중에는 검증 가능하게(D) 만드는 도구. 이름의 뜻: 개인신용정보의 결합(結)이 일어나는 지점에 치는 경계(界).

## 구성요소

| 코드 | 이름 | 역할 |
|---|---|---|
| W | 판정 워크벤치 | 도입 전 예외요건 검토, 조치 제안, 검토서 생성 |
| D | 결계 검증 엔진 | 개인신용정보 유입의 실시간·주기 탐지 (3층) |
| E | 근거 추출 파이프라인 | 벤더 공개 문서에서 통제 수단을 원문 인용과 함께 추출 |
| L | 자가보강 루프 | D의 성능 측정·개선. 생성이 곧 라벨 (PurpleBPF 방법론 이식) |

## 파이프라인

### A. 도입 전 판정 (W+E)

```
회사 프로필(업권·E5 보유) + SaaS·용도 선택
  → [E] LLM이 공개 관리자 문서·DPA에서 층별 통제 수단 추출 (원문 인용 필수)
  → 담당자 확인 체크 (사람 게이트)
  → 결정 트리 판정 (결정적)
       고객정보가 업무 목적 / L1 필드형 사용 → 불가
       유입 가능성 등급 × 통제 상태 → 적합 / 조건부 / 불가
  → 미해소 위험마다 조치 제안 (콘솔 설정 경로, 보완통제 지정, 내규 문안)
       프로필 반영: E5 미보유면 보완통제(D) 경로 제안
  → 검토서 생성 → 도입 품의 · 정보보호위원회 승인 자료
```

### B. 운영 중 검증 (D)

```
실시간: 브라우저 확장 / 모의 편집기 → 입력 텍스트 → 3층 탐지 → 전송 전 경고 + 로그
주기:   저장 문서·녹취록 배치 스캔 → 위반 리포트 (해설서 "로그 주기 점검" 구현)
로그·통계 → 반기 자체평가 증적 → 다음 판정(A)의 입력으로 환류
```

W와 D의 결합점: 벤더 통제 미제공 시(예: Notion DLP 없음) 검토서가 D를 보완통제로 지정.
해설서 "기능이 없는 경우 보완 보안대책 적용"의 실물.

### C. 자가보강 루프 (L)

```
생성기(LLM): 합성 금융 문서 생성, 위반 심은 위치 = y_true (생성이 곧 라벨)
  → D 스캔 = y_pred → TP/FN/FP → 정밀도·재현율
  → FN 유형을 생성기 목표로 재투입 (어려운 케이스 증식) → 규칙·프롬프트 보강
산출: 방법별 정확도 표(규칙/패턴DLP/소형LLM × 모델 3종), 세대별 재현율 곡선, 합성 데이터셋 공개
```

## 검증 엔진 D의 3층

| 층 | 대상 | 방법 | 위치 |
|---|---|---|---|
| 규칙 | 주민번호(체크섬), 계좌·카드(Luhn), 여권·전화 | 정규식+검증, 결정적 | 클라이언트 가능 |
| NER | 이름·주소·연락처 등 식별정보 | 한국어 개체명 인식 | 로컬 |
| 결합 | 식별정보 × 거래·대출·연체·상담 문맥의 결합 | 소형 LLM (기본 EXAONE 3.5, 비교: gemma3·llama) | 로컬 (Ollama) |

위반 확정 규칙 = 신용정보법 제2조의 코드화:
고유식별정보 검출은 그 자체로 위반 / 식별정보 단독은 통과(기록) / 식별정보+신용정보 결합은 위반.
셋째 줄이 기존 DLP가 구조적으로 못 하고 LLM이 필수인 지점.

## 불변 원칙

1. 판정·위반 확정은 결정적 로직만. LLM 출력은 후보+근거이며 사람 확인 또는 규칙 통과 후에만 효력
2. 모든 AI 출력은 원문 인용 필수. 인용 없으면 폐기
3. 민감정보의 처리 위치는 로컬. 모델이 데이터에게로 간다

## 배포

```
실배포: 금융사 내부망 · Ollama + 소형 모델 · 브라우저 확장 + 배치 스캐너 + 워크벤치
데모:   GitHub Pages(정적, 다운타임 0) + Cloudflare Worker(키 은닉·속도 제한)
        + 호스팅 오픈모델(공급자 이중화). 규칙층은 클라이언트라 서버 전멸에도 실시간 탐지 유지.
        모델층 장애 시 규칙층+안내로 강등. 확장은 실동작 영상 임베드로 증명.
```

## 대회 포지셔닝 (2025 수상 패턴 반영)

- 조치 제안까지: 판정에서 끝나지 않고 설정 경로·보완통제·내규 문안 제안 (SIGNAL 승리 공식)
- 회사 프로필 = B2B 초개인화: 업권·요금제 보유에 따라 조치가 달라짐
- 국산 모델 서사: EXAONE 기본 채택 + 외산 대비 측정 (뛰어 팀의 원장상 공식)
- 대화형 인테이크(계획): 자유 서술 → 비서가 문답으로 판정 입력 채움 (공고 예시 7 결)
- 보호 대상은 금융소비자: 사용자(담당자)와 보호 대상(소비자 개인신용정보)의 분리 명시
- 주제 계열 선언: 공고 예시 6(이상거래 탐지)·7(AI 보안 대응 비서)의 금융회사 보안 실무 지원 계열
- 과녁: 본선 11팀 진입 → 금보원 원장상·공동개최사 대표이사상 권역

## 산출물

| 출구 | 내용 | 마감 |
|---|---|---|
| 금융 AI Challenge | W+D 데모 URL + 기획서(정확도 표·루프 곡선) + 기능명세서 | 9/7 10:00 |
| CISC-W'26 논문 | 결합 탐지 방법 비교 + 3층 프레임워크 + SaaS 통제 실측 | 9월 말 추정 |
| 공개 | 규칙층 룰팩, 합성 데이터셋, 검토서 서식 | 수시 |

## 4주 우선순위

1. D 규칙층+결합층, L 한 바퀴 (정확도 표·루프 곡선 확보)
2. 조치 제안 레이어 (SaaS 5종 × 위험 유형별 템플릿)
3. W에 D 통합(모의 편집기), 회사 프로필, 데모 배포(Pages+Worker)
4. 대화형 인테이크, E 실시간화, 크롬 확장 영상 (여유 시)
